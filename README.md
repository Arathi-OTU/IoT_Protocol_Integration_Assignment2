# IoT_Protocol_Integration_Assignment2

This assignment aims to build the IoT data pipeline from scratch, connecting physical-layer sensors to a backend broker using two different protocols, comparing their behavior, and implementing the backend routing and processing logic.
